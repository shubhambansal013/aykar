// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: common/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "tax.common";

export interface DocumentMetadata {
  /** e.g., "2025-26" */
  financialYear: string;
  /** e.g., "CYXPA6852K202607050242" */
  downloadId?:
    | string
    | undefined;
  /** Generation timestamp */
  generationDate:
    | Date
    | undefined;
  /** Client IP captured on download */
  ipAddress?: string | undefined;
}

export interface TaxpayerProfile {
  /** Permanent Account Number */
  pan: string;
  /** Masked Aadhaar (e.g., "XXXX XXXX 0899") */
  aadhaarMasked?:
    | string
    | undefined;
  /** Name of Assessee */
  name: string;
  /** Format: DD/MM/YYYY or YYYY-MM-DD */
  dateOfBirth: string;
  /** 10-digit mobile number */
  mobileNumber?:
    | string
    | undefined;
  /** Taxpayer email */
  emailAddress?:
    | string
    | undefined;
  /** Full residential address */
  address?: string | undefined;
}

function createBaseDocumentMetadata(): DocumentMetadata {
  return { financialYear: "", downloadId: undefined, generationDate: undefined, ipAddress: undefined };
}

export const DocumentMetadata: MessageFns<DocumentMetadata> = {
  encode(message: DocumentMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.financialYear !== "") {
      writer.uint32(10).string(message.financialYear);
    }
    if (message.downloadId !== undefined) {
      writer.uint32(18).string(message.downloadId);
    }
    if (message.generationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.generationDate), writer.uint32(26).fork()).join();
    }
    if (message.ipAddress !== undefined) {
      writer.uint32(34).string(message.ipAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DocumentMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDocumentMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.financialYear = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.downloadId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.generationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ipAddress = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DocumentMetadata {
    return {
      financialYear: isSet(object.financialYear)
        ? globalThis.String(object.financialYear)
        : isSet(object.financial_year)
        ? globalThis.String(object.financial_year)
        : "",
      downloadId: isSet(object.downloadId)
        ? globalThis.String(object.downloadId)
        : isSet(object.download_id)
        ? globalThis.String(object.download_id)
        : undefined,
      generationDate: isSet(object.generationDate)
        ? fromJsonTimestamp(object.generationDate)
        : isSet(object.generation_date)
        ? fromJsonTimestamp(object.generation_date)
        : undefined,
      ipAddress: isSet(object.ipAddress)
        ? globalThis.String(object.ipAddress)
        : isSet(object.ip_address)
        ? globalThis.String(object.ip_address)
        : undefined,
    };
  },

  toJSON(message: DocumentMetadata): unknown {
    const obj: any = {};
    if (message.financialYear !== "") {
      obj.financialYear = message.financialYear;
    }
    if (message.downloadId !== undefined) {
      obj.downloadId = message.downloadId;
    }
    if (message.generationDate !== undefined) {
      obj.generationDate = message.generationDate.toISOString();
    }
    if (message.ipAddress !== undefined) {
      obj.ipAddress = message.ipAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DocumentMetadata>, I>>(base?: I): DocumentMetadata {
    return DocumentMetadata.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DocumentMetadata>, I>>(object: I): DocumentMetadata {
    const message = createBaseDocumentMetadata();
    message.financialYear = object.financialYear ?? "";
    message.downloadId = object.downloadId ?? undefined;
    message.generationDate = object.generationDate ?? undefined;
    message.ipAddress = object.ipAddress ?? undefined;
    return message;
  },
};

function createBaseTaxpayerProfile(): TaxpayerProfile {
  return {
    pan: "",
    aadhaarMasked: undefined,
    name: "",
    dateOfBirth: "",
    mobileNumber: undefined,
    emailAddress: undefined,
    address: undefined,
  };
}

export const TaxpayerProfile: MessageFns<TaxpayerProfile> = {
  encode(message: TaxpayerProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pan !== "") {
      writer.uint32(10).string(message.pan);
    }
    if (message.aadhaarMasked !== undefined) {
      writer.uint32(18).string(message.aadhaarMasked);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.dateOfBirth !== "") {
      writer.uint32(34).string(message.dateOfBirth);
    }
    if (message.mobileNumber !== undefined) {
      writer.uint32(42).string(message.mobileNumber);
    }
    if (message.emailAddress !== undefined) {
      writer.uint32(50).string(message.emailAddress);
    }
    if (message.address !== undefined) {
      writer.uint32(58).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TaxpayerProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTaxpayerProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pan = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.aadhaarMasked = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dateOfBirth = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mobileNumber = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.emailAddress = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TaxpayerProfile {
    return {
      pan: isSet(object.pan) ? globalThis.String(object.pan) : "",
      aadhaarMasked: isSet(object.aadhaarMasked)
        ? globalThis.String(object.aadhaarMasked)
        : isSet(object.aadhaar_masked)
        ? globalThis.String(object.aadhaar_masked)
        : undefined,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      dateOfBirth: isSet(object.dateOfBirth)
        ? globalThis.String(object.dateOfBirth)
        : isSet(object.date_of_birth)
        ? globalThis.String(object.date_of_birth)
        : "",
      mobileNumber: isSet(object.mobileNumber)
        ? globalThis.String(object.mobileNumber)
        : isSet(object.mobile_number)
        ? globalThis.String(object.mobile_number)
        : undefined,
      emailAddress: isSet(object.emailAddress)
        ? globalThis.String(object.emailAddress)
        : isSet(object.email_address)
        ? globalThis.String(object.email_address)
        : undefined,
      address: isSet(object.address) ? globalThis.String(object.address) : undefined,
    };
  },

  toJSON(message: TaxpayerProfile): unknown {
    const obj: any = {};
    if (message.pan !== "") {
      obj.pan = message.pan;
    }
    if (message.aadhaarMasked !== undefined) {
      obj.aadhaarMasked = message.aadhaarMasked;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.dateOfBirth !== "") {
      obj.dateOfBirth = message.dateOfBirth;
    }
    if (message.mobileNumber !== undefined) {
      obj.mobileNumber = message.mobileNumber;
    }
    if (message.emailAddress !== undefined) {
      obj.emailAddress = message.emailAddress;
    }
    if (message.address !== undefined) {
      obj.address = message.address;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TaxpayerProfile>, I>>(base?: I): TaxpayerProfile {
    return TaxpayerProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TaxpayerProfile>, I>>(object: I): TaxpayerProfile {
    const message = createBaseTaxpayerProfile();
    message.pan = object.pan ?? "";
    message.aadhaarMasked = object.aadhaarMasked ?? undefined;
    message.name = object.name ?? "";
    message.dateOfBirth = object.dateOfBirth ?? "";
    message.mobileNumber = object.mobileNumber ?? undefined;
    message.emailAddress = object.emailAddress ?? undefined;
    message.address = object.address ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
